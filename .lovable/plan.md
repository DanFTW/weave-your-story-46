

## Redeploy email-text-alert Edge Function

No code changes needed. The function code already includes the SMS sending implementation — it just needs to be redeployed so the live version matches.

### Steps
1. Deploy `email-text-alert` edge function using the Supabase deploy tool
2. Verify deployment by checking edge function logs

