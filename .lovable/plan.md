

## Redeploy weekly-event-finder edge function

Single step: redeploy the `weekly-event-finder` edge function to push the latest code (the `entity_id` + `arguments` fix) live, then verify it responds correctly.

No code changes needed — just deployment.

